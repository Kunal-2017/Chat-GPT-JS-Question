// area of rectangle
/*
let length = Number(prompt("Enter the length of rectangle"));
let breadth = Number(prompt("Enter the breadth of rectangle"));

document.write("Area of rectangle whose length " + length + " and breadth " + breadth + " is " + (length * breadth) + "<br>");*/


// area of square

let side = Number(prompt("Enter the side of a square"));
document.write(`Area of square whose side ${side} is ${side * side}.<br>`);
document.write(`Perimeter of square whose side ${side} is ${side * 4}.<br>`);

// area of circle


// let radius = Number(prompt("Enter the radius of circle"));
// document.write(`Area of circle whose radius ${radius} is ${Math.PI * radius * radius}`);

